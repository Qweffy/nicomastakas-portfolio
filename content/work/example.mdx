---
title: Example Case Study
summary: Template case study that validates the Velite content pipeline. Replace with a real one or delete.
role: Product Engineer
stack:
  - Next.js
  - TypeScript
date: 2026-06-17
draft: true
---

This placeholder exists only to exercise the MDX content collection (frontmatter
schema, typed export, Shiki highlighting). It is `draft: true`, so it never ships
to the index or sitemap. Delete it once a real case study lands.

```ts
// Code blocks are highlighted at build time with Shiki (zero runtime JS).
export function greet(name: string): string {
  return `Hello, ${name}`;
}
```
